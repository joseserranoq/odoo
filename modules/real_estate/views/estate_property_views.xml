<odoo>
    <!-- Para crear la accion de ventana-->
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Estate Property</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- Para ensenar loos titulos de la aplicacion en UI-->
    <menuitem id="real_estate_menu_root" name="Real Estate root">
        <menuitem id="first_level_menu" name="Real Estate First Level">
            <menuitem id="estate_property_menu_action" action="estate_property_action"></menuitem>
        </menuitem>
    </menuitem>

    <!-- Para poner el titulo a la tabla-->
    <record id="estate_property_tree_view" model="ir.ui.view">
        <field name="name">estate.property.tree</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"></field>
                <field name="postcode"></field>
                <field name="bedrooms"></field>
                <field name="living_area"></field>
                <field name="expected_price"></field>
                <field name="selling_price"></field>
                <field name="date_availability"></field>
            </list>
        </field>
    </record>

    <!-- cuando se selecciona el objeto aparece esta vista formulario-->
    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form name="Description">
                <sheet>
                    <h1 class="oe_title">
                        <field name="name"></field>
                    </h1>
                    <group>
                        <group>
                            <field name="postcode"></field>
                            <field name="date_availability"></field>
                        </group>
                        <group>
                            <field name="expected_price"></field>
                            <field name="selling_price"></field>
                        </group>
                    </group>
                    <notebook>
                        <page string="Details">
                            <group>
                                <field name="description"></field>
                                <field name="bedrooms"></field>
                                <field name="living_area"></field>
                                <field name="facades"></field>
                                <field name="garage"></field>
                                <field name="garden"></field>
                                <field name="garden_area"></field>
                                <field name="garden_orientation"></field>
                                <field name="state"></field>
                                <field name="active"></field>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>
<!-- XML does not allow < and & to be used inside XML elements. 
 To avoid parsing errors, entity references should be used: &lt; for < and &amp; 
 for &. Other entity references (&gt;, &apos; & &quot;) are optional.-->
    <record id="estate_property_search_view" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Busqueda de propiedades">
                <filter name="name" string="Property Name" />
                <filter name="postcode" string="Postcode" />
                <filter name="expected_price" string="Expected Price > 10" domain="[('expected_price','>',10)]"/>
                <!-- The logical operators & (AND), | (OR) and ! (NOT) 
                 can be used to explicitly combine criteria. They are used in prefix position 
                 (the operator is inserted before its arguments rather than between)-->
                <filter name="state" string="Available properties" domain="['|',('state', '=','new'),('state','=','offer_received')]"/>
                <separator/>
                <filter name="group_by_postcode" string="Postcode" context="{'group_by':'postcode'}"/>
            </search>
        </field>
    </record>
</odoo>